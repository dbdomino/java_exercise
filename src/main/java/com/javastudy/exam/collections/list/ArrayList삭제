ArrayList에 저장된 객체의 삭제 과정
- ArrayList에 저장된 세 번째 데이터(data[2])를 삭제하는 과정
- list.remove(2);를 호출

    1. 삭제할 데이터 뒤의 데이터를 한칸씩 위로 복사해서 삭제할 데이터를 덮어쓴다. (부담이 많이 가므로 조심해서 사용)
    data[2] = data[3]
    data[3] = data[4]
    data[4] = data[5]
    .......
    2. 마지막 데이터는 null로 변경한다.
    3. 데이터가 삭제되어 데이터의 개수가 줄었으므로, size의 값을 감소시킨다.

    참고, 마지막 데이터를 삭제하는 경우 1.의 과정(배열의 덮어쓰기)는 필요없다.)

- 부담안가게 ArrayList의 데이터를 삭제하려면 마지막 데이터 순서로 삭제하면 된다. 빠르다. (배열의 복사, 덮어쓰기는 발생하지 않기 때문)
- 그러나 ArrayList의 데이터 첫번째부터 삭제시키는거면 부하가 생길 수 있다.
