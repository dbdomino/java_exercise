package com.javastudy.exam.stream.test;

public class GrouppingMappingEx {
    public static void main(String[] args) {
        /*
        Map<String, List<A>> a = orders.stream()
            .collect(Collectors.groupingBy(A::getDate));
                    결국 A 클래스 리스트를 가지게 되겠고
        List<Date> a = orders.stream()
            .map(order -> order.getDate() * 2)
            .collect(Collectors.toList());


         */
    }
}
